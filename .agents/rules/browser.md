# Browser Usage Rule

**NEVER** initiate or trigger the `browser_subagent` (the DOM agent) autonomously for testing or verification unless the USER explicitly asks you to test the UI, open a browser, or interact with a page. By default, rely exclusively on terminal logs, unit tests, and programmatic tools for verification.
